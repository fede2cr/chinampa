[build]
target = "index.html"
# Served from the domain root (custom domain https://chinampa.co.cr/).
# For a GitHub Pages project site at /<repo>/, set public_url = "/<repo>/" and
# update BASE / DATA_BASE in src/app.rs to match.
public_url = "/"
